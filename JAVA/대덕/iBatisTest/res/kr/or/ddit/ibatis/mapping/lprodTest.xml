<?xml version="1.0" encoding="UTF-8"?>

<!-- 이 문서는 iBatis에서 처리할 SQL문을 작성하는 문서입니다. -->
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
	
<sqlMap namespace="Lprod">
	<!-- 
		이 부분에 처리할 SQL문에 맞는 태그를 사용해서 처리할 SQL문을 기술한다.
		
		사용할 수 있는 기본적인 태그들...
		<select> 쿼리문 </select>
	 	<insert> 쿼리문 </insert>
	 	<update> 쿼리문 </update>
	 	<delete> 쿼리문 </delete>
	 -->
	 
	 <!-- 
	 	id속성 ==> 실행할 태그를 Java소스에서 호출할 때 사용되는 이름으로 <sqlMap>태그의 namespace속성값과 연결해서 사용한다.
	 	예)"Lprod.insertLprod"
	 	parameterClass속성 ==> SQL문에 사용할 데이터가 들어있는 객체를 기술한다. 
	 	(보통 VO클래스, Map객체, Java의 기본자료형이름 등이 사용된다)
	 	(VO클래스 등을 기술할 때는 해당 클래스의 전체 이름을 모두 기술해야함)
	  -->
	  <!--  <insert id="insertLprod" parameterClass="kr.or.ddit.basic.vo.LprodVO">  -->
	 <insert id="insertLprod" parameterClass="lprodVo">
	 	insert into lprod(lprod_id, lprod_gu, lprod_nm)
	 	values(#lprod_id#, #lprod_gu#, #lprod_nm#)
	 </insert>
</sqlMap>
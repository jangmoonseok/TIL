# Method(메서드)
* 객체에 저장된 값이 함수인 경우, 이를 메서드(method)라고 부름

![image](https://user-images.githubusercontent.com/64582209/134893072-6ebfdfdd-9f7e-4e22-be22-fbc12581cd96.png)

## method 변경
* method변경: 객체 내 초기 선언된 함수를 다른 함수로 변경

```
function hello_func(){
    console.log("hello");
}

function hi_func(){
    console.log("hi");
}

let obj = {
    name:"john",
    age:27,
    func:hello_func
}

hello_func(); //hello
hi_func(); // hi
obj.func(); // hello

obj.func = hi_func
obj.func(); // hi
```
* obj의 func속성이 가리키는 함수를 hello_func에서 hi_func로 변경

## this
* 메서드에서 객체 내부의 속성값을 접근할 수 있는 지시자

![image](https://user-images.githubusercontent.com/64582209/134894164-6b19d99a-5909-428b-953c-cfb175b42fd3.png)
```
let user = {name:"john"};
let admin = {name:"admin"};

function hello_func(){
    console.log("Hello " + this.name);
}

user.func = hello_func;
admin.func = hello_func;

user.func(); // Hello john
admin.func(); // Hello admin
```
* hello_func함수가 user객체와 admin객체의 메서드가 돼서 객체 내부에 name속성에 접근하기 위해 this사용

## 문자열 치환
* 처음 만나는 요소 문자열 치환(치환된 문자열을 반환) : String.replace(origin_str, change_str)
* 정규 표현식 활용 문자열 치환 : 치환 문자열에 정규 표현식 기입 >> /치환문자열/g(전체)i(대소문자 구분X)
![image](https://user-images.githubusercontent.com/64582209/134898308-dbe511cc-dd8f-497f-a8bd-d5483a7b23eb.png)

## 문자열 추출
* 위치 기반 문자열 추출 : String.slice(start,end), String.substring(start,end)
* 길이 기반 문자열 추출 : String.substr(start,length)

![image](https://user-images.githubusercontent.com/64582209/134899000-7319698d-2025-4497-8261-c6976fedc379.png)

## 문자열 분할
* 배열로 문자열 분할 : String.split(Separator, limit)


![image](https://user-images.githubusercontent.com/64582209/134899290-40b54494-5563-446d-ad76-4445242d7c26.png)


# Array(배열)

![image](https://user-images.githubusercontent.com/64582209/134899633-afc681c3-1769-4413-8b31-9eb20fc2d446.png)

## 배열 선언/접근/속성
* 선언 : new Array() 혹은  []를 통해 선언하며, 사이즈 혹은 값을 입력하여 초기화도 가능
* 접근 방법 : Array[index]를 통해 index를 통하여 접근
* 배열 속성 : Array.length를 통해 배열 요소의 개수 확인

![image](https://user-images.githubusercontent.com/64582209/134900077-71caa66c-7965-448d-a837-50315da4b6d6.png)

## 배열의 실체
* 자바스크립트에서 배열은 다른 언어에서 말하는 일반적인 배열이 아닌 Hash 기반의 객체
* 메모리가 연속적인 밀집 배열(dense array)이 아닌 비 연속적인 희소 배열(sparse array)

![image](https://user-images.githubusercontent.com/64582209/134900793-3d19547c-0877-4932-82e1-cb8070bededa.png)

## 배열 타입 확인 및 요소 삭제
### 배열 타입 확인
* Array.isArray(value)

```
let num = 123.456;
let str = "Here I am!";
let fruits = ["apple", "orange", "melon"];

console.log(Array.isArray(num)); // false
console.log(Array.isArray(str)); // false
console.log(Array.isArray(fruits)); // true
```


### 배열 요소 삭제
* 배열 일부 요소 삭제 : delete array[index] (삭제를해도 배열 사이즈가 그대로인 문제점이있다.)
```

let fruits = ["apple", "orange", "melon"];

console.log(fruits); // [ 'apple', 'orange', 'melon' ]
console.log(fruits.length); // 3

delete fruits[1];
console.log(fruits); // [ 'apple', <1 empty item>, 'melon' ]
console.log(fruits.length); // 3
```
* 배열의 요소를 삭제할 때 delete는 사용하지 않는걸 권장


## 배열 조작

 ### 추가/삭제
1. LIFO-BACK(배열의 뒤에서 이루어짐)
* 배열 추가 : Array.push(element), 배열 삭제 : Array.pop()
```

let fruits = ["apple", "orange", "melon"];
let ret;
ret = fruits.push("watermelon");
console.log(fruits); // [ 'apple', 'orange', 'melon', 'watermelon' ]
console.log(ret); // 4, push메서드에 대한 return값은 배열의 길이이다.

ret = fruits.pop();
console.log(fruits); // [ 'apple', 'orange', 'melon' ]
console.log(ret) // watermelon, pop메서드에 대한 return값은 삭제된 요소이다.

```
2. LIFO-FRONT(배열의 앞에서 이루어짐)
* 배열 추가 : Array.unshift(elemet), 배열삭제 : Array.shift()
```

let fruits = ["apple", "orange", "melon"];
let ret;
ret = fruits.unshift("watermelon");
console.log(fruits); // [ 'watermelon', 'apple', 'orange', 'melon' ]
console.log(ret); // 4, unshift메서드에 대한 return값은 배열의 길이이다.

ret = fruits.shift();
console.log(fruits); // [ 'apple', 'orange', 'melon' ]
console.log(ret) // watermelon, shift메서드에 대한 return값은 삭제된 요소이다.

```
### 배열 삭제/변경(index)
* 배열 요소 삭제/변경 : Array.splice(index, deleteCount, elem1, elem2,...)
```

let fruits = ["apple", "orange", "melon"];
let ret;

ret = fruits.splice(1,1);
console.log(fruits); // [ 'apple', 'melon' ], splice메서드를 실행하면 원본 배열이 변경된다.
console.log(ret); // [ 'orange' ], splice메서드에 대한 return값은 splice된 요소이다.

ret = fruits.splice(1,1,"mango", "kiwi");
console.log(fruits); // [ 'apple', 'mango', 'kiwi' ]
console.log(ret); // [ 'melon' ]
```

### 배열 삭제(index)
* 배열 요소 삭제 : Array.slice([start], [end])
```

let fruits = ["apple", "orange", "melon"];
let ret

ret = fruits.slice(1,2);
console.log(ret); // [ 'orange' ], index 1부터 index 2가 되기 전 까지 slice하겠다.
console.log(fruits); // [ 'apple', 'orange', 'melon' ], splice와 다르게 원본 배열에는 영향이없다.
```
### 배열 병합
* 다중 배열 병합 : Array.concat(arg1, arg2...)
```

let fruits = ["apple", "orange", "melon"];
let fruits_add = ["cherry", "banana"];
let ret;

ret = fruits.concat(fruits_add);
console.log(ret); // [ 'apple', 'orange', 'melon', 'cherry', 'banana' ]
console.log(fruits); //[ 'apple', 'orange', 'melon' ], concat메서드는 원본 배열에 영향이 없다.
```
### 배열 반복문
* 다양한 반복문 문법을 통해 배열 요소에 접근 가능

```

let fruits = ["apple", "orange", "melon"];

// 1. use for
for(let i = 0; i < fruits.length; i++){
    console.log(fruits[i])
}
// apple, orange, melon

// 2. use for..(element) of
for(let fruit of fruits){
    console.log(fruit)
}
// apple, orange, melon

// 3. use for..(key) in
for(let key in fruits){
    console.log(key) // 0,1,2
    console.log(fruits[key]) // apple, orange, melon
}
```

## 배열 탐색
* index 탐색(앞에서부터) : Array.indexOf(item,from)
* index 탐색(뒤에서부터) : Array.lastIndexOf(item,from)
* 값 포함 여부 확인 : Array.includes(item, from)

![image](https://user-images.githubusercontent.com/64582209/134906489-ebd17f8c-12b0-47bb-84fa-f16fca8c4998.png)

## 배열 변형

### 배열 정렬
* 내림차순 정렬 : Array.sort(), 오름차순 정렬 : Array.reverse()

![image](https://user-images.githubusercontent.com/64582209/134906937-9dd6b6d7-d6aa-40b2-a103-08b47b9953d5.png)

### 배열 변환
* 배열 값을 문자로 변환 : Array.join(separator)

![image](https://user-images.githubusercontent.com/64582209/134906998-3293d679-5d2d-4035-bbb8-28ba8582afa8.png)


# 선형 자료구조(배열)
![image](https://user-images.githubusercontent.com/64582209/135583724-c4607ac0-9b90-4e58-8540-519388f2f247.png)

## some()
* 배열 내 단 하나라도 콜백 함수의 조건을 만족하는 요소가 있다면 true, 아니면 false반환( 빈 배열이면 false)
* Array.some(function(item,index,array){})
```
let users = [
    {name:"bob", age:17, job:false},
    {name:"alice", age:20, job:false},
    {name:"john", age:27, job:true}
]

let some_job = users.some(user => {
    return user.job == false
})
console.log(some_job) // true

let some_age = users.some(user => {
    return user.age < 16
})
console.log(some_age) // false

let empty = [].some(item => item > 16)
console.log(empty) // false
```


## every()
* 배열 내 모든 요소가 콜백 함수의 조건을 만족한다면 true, 아니면 false( 빈 배열인 경우 true)
* Array.every(function(item,index,arr){})

```
let users = [
    {name:"bob", age:17, job:false},
    {name:"alice", age:20, job:false},
    {name:"john", age:27, job:true}
]

let some_job = users.every(user => {
    return user.job == false
})
console.log(some_job) // true

let some_age = users.every(user => {
    return user.age > 16
})
console.log(some_age) // false

let empty = [].every(item => item > 16)
console.log(empty) // false
```

## 배열 문제풀이

### 1번
![image](https://user-images.githubusercontent.com/64582209/135587412-a3108a5a-6221-4a03-af0b-73d379c14c7f.png)

#### 풀이
```
function answer(nums){
    let result = [];
    // 최소값 구하기 //
    // let min_num = Math.min.apply(null, nums) //
    let min_num = Number.MAX_SAFE_INTEGER;
    for(let i = 0; i< nums.length; i++){
        if(min_num > nums[i]){
            min_num = nums[i]
        }
    }
    // min을가진 index를 배열로 반환 //
    for(let i = 0; i < nums.length; i++){
        if(nums[i] == min_num) result.push(i)
    }
    console.log(result)
}

let input = [
    [5,2,10,2],
    [4,5,7,4,8],
    [12,11,11,16,11,12]
]

for(let i = 0; i < input.length; i++){
    answer(input[i])
}
/*
[ 1, 3 ]
[ 0, 3 ]
[ 1, 2, 4 ]
*/
```

### 2번
![image](https://user-images.githubusercontent.com/64582209/135589879-804d8b49-000f-41fc-8ba6-65d8c0e15762.png)

#### 풀이
```
function answer(chess){
    let result = []
    let chess_set = [1,1,2,2,2,8]
    for(let i = 0; i < chess.length; i++){
        result.push(chess_set[i] - chess[i])
    }
    console.log(result)

}

let input = [
    [0,1,2,2,2,7],
    [2,1,2,1,2,1],
    [0,1,1,5,3,6]
]

for(let i = 0; i < input.length; i++){
    answer(input[i])
}
```

### 3번
![image](https://user-images.githubusercontent.com/64582209/135590840-fa49a72e-a6ff-4cc5-88a6-09ccaeead25d.png)
#### 풀이
```
function answer(nums){
    /* 나의 풀이 
    nums에서 제일 큰 수를 뺀 반환 된 배열에서 또 제일 큰 수를 찾는다. 
    let result = []
    for(let i = 0; i < 2; i++){
        let max = Math.max.apply(null, nums)
        result.push(max)
        nums.splice(nums.indexOf(max),1)
    }
  */
    let result = []
    // result[0] -> 첫번째 최대값 , result[1] -> 두번째 최대값
    result = nums[0] > nums[1] ? [nums[0], nums[1]] : [nums[1], nums[0]]
    for(let i = 2; i < nums.length; i++){
        // index 2 부터 배열을 돌면서 result배열의 요소와 비교를 하고 스왑
        if(nums[i] > result[0]){
            result[1] = result[0]
            result[0] = nums[i]
        }else if(nums[i] > result[1]){
            result[1] = nums[i]
        }
    }
    console.log(result)
}

let input = [
    [-11,5,18,-2,-3,6,4,17,10,9],
    [3,7,-14,2,-6,13,-20,-2,-7,6,-17,-5,14,-9,19],
    [-15,-4,-8,12,12,-8,-8,9,10,15,-2,10,-14,2,13,19,-9,3,-18,14]
]

for(let i = 0; i < input.length; i++){
    answer(input[i])
}
/*
[ 18, 17 ]
[ 19, 14 ]
[ 19, 15 ]
*/
```
### 4번
![image](https://user-images.githubusercontent.com/64582209/135597086-f4714a63-61bd-43b0-99df-95e8f8b42906.png)
#### 풀이
```
function answer(nums){
    let result = []

    let sum = 0;
    for(let i = 0; i < nums.length; i++){
        sum += nums[i]
    }
    sum -= 100
    let faker = []
    for(let i = 0; i < nums.length; i++){
        for(let j = i + 1; j < nums.length; j++){
            if(nums[i] + nums[j] == sum) {
                faker.push(i, j)
                break
            }
        }
        if(faker.length != 0) break
    }

    for(let i = 0; i < nums.length; i++){
        if(faker[0] != i && faker[1] != i){
            result.push(nums[i])
        }
    }

    console.log(result)
}

let input = [
    [1,5,6,7,10,12,19,29,33],
    [25,23,11,2,18,3,28,6,37],
    [3,37,5,36,6,22,19,2,28]
]

for(let i = 0; i < input.length; i++){
    answer(input[i])
}

```
